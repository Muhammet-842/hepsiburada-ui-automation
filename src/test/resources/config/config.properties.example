base.url=https://www.hepsiburada.com
timeout.explicit.seconds=15
timeout.pageload.seconds=30
headless=false
browser=chrome
screenshot.dir=reports/screenshots
post.scenario.pause.seconds=5
chrome.profile.dir=chrome-profile

# Kullanici adi/sifre BURAYA YAZILMAZ.
# Once ortam degiskeni HB_USERNAME / HB_PASSWORD okunur,
# bulunamazsa bu dosyadaki hb.username / hb.password fallback olarak kullanilir (local test icin, gitignore'da).
hb.username=
hb.password=
